export default interface ProviderData {
    id?: any | null,
    guid?: string | null,
    name?: string | null,
    phoneNumber?: string | null,
    active?: boolean,
    createdAt?: string | null
}
